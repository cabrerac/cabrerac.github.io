# Core dependencies
nbformat==5.9.2  # For Jupyter notebook generation
pyyaml==6.0.1    # For YAML front matter parsing
pillow==10.1.0   # For image processing
python-magic-bin==0.4.14  # For file type detection (Windows compatible)

# Development dependencies
pytest==7.4.3    # For testing
black==23.11.0   # For code formatting
flake8==6.1.0    # For linting
mypy==1.7.1      # For type checking

# Optional dependencies
jupyter==1.0.0   # For notebook preview
ipykernel==6.25.1  # For Jupyter kernel support
PyMuPDF>=1.24.10  # For scripts/pdf_to_markdown; >=1.27.2 when using pymupdf4llm
pymupdf4llm>=1.0.0  # Optional: use with --llm for tables/lists/better structure
python-pptx>=0.6.23  # project presentation template (build_project_presentation.py)

# Google Cloud dependencies
google-api-python-client
google-auth-httplib2
google-auth-oauthlib
