name: Build publications

on:
  push:
    branches:
      - gh-pages
    paths:
      - _data/publications.yml
      - scripts/publications/**
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install pyyaml==6.0.1

      - name: Build BibTeX from publications.yml
        run: python scripts/publications/build.py

      - name: Commit generated bibfile
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Build bibfile from publications.yml"
          file_pattern: assets/bibs/bibfile.bib _data/publications.yml
